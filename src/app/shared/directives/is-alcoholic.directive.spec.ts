import { IsAlcoholicDirective } from './is-alcoholic.directive';

describe('IsAlcoholicDirective', () => {
  it('should create an instance', () => {
    const directive = new IsAlcoholicDirective();
    expect(directive).toBeTruthy();
  });
});
